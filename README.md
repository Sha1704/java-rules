# IT-355

## Running the Project (Windows)

To compile and run the Java application with the Oracle JDBC driver:

```bash
javac -cp ".;ojdbc17.jar" app/Main.java
java  -cp ".;ojdbc17.jar" app.Main

use rm app/*.class to clean up excess .class file

you have to be connected to the ISU's wifi to suscessfully run the program

Id is 1